# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2
project_name: certinfo

before:
  hooks:
    - go mod tidy

builds:
  - id: certinfo
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    binary: certinfo
    main: ./main.go
    ldflags:
      - -s -w

archives:
  - id: certinfo
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "{{ .ProjectName }}_checksums.txt"
  algorithm: sha256

# Ritorno alle Formulae (brews)
brews:
  - name: certinfo
    repository:
      owner: marco-introini
      name: homebrew-tap
    directory: Formula
    homepage: https://github.com/marco-introini/certinfo
    description: CLI tool to analyze X.509 certificates and private keys
    license: MIT
    commit_author:
      name: marco-introini
      email: marco@marcointroini.it
    # Non serve 'binaries' qui, viene gestito automaticamente dai 'builds'

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  footer: |
    ---
    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).